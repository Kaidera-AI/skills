{
  "name": "skills",
  "version": "1.0.0",
  "description": "Kaidera Skills Marketplace — locally validated skill manifests for AI agents",
  "private": true,
  "scripts": {
    "validate": "node scripts/validate-skill.js",
    "generate": "node scripts/generate-marketplace.js",
    "scan": "python3 scripts/security-scan.py",
    "review-contract": "node scripts/open-code-review-contract.js",
    "test": "node scripts/test-open-code-review.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "yaml": "2.9.0"
  }
}
